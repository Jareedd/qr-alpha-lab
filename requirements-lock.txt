# Exact versions the documented reproduction was verified against.
#
# requirements.txt states the SUPPORTED RANGE (>=); this file states the ONE
# combination `scripts/reproduce.py` was actually run on, so a reviewer who
# hits a numeric mismatch can rule the environment in or out in one step
# rather than guessing.
#
#   pip install -r requirements-lock.txt     # exactly what was verified
#   pip install -r requirements.txt          # supported ranges, newer libs OK
#
# Verified on: python 3.11.15, Linux x86_64, 2026-09-08.
# Regenerate with: pip freeze (system packages filtered out).
#
# Numerically load-bearing: numpy, scipy, scikit-learn (BLAS-dependent float
# accumulation), pandas (groupby/rank ordering), pyarrow (parquet round-trip).
# The rest are I/O and plotting.
Pygments==2.21.0
beautifulsoup4==4.15.0
certifi==2026.2.25
charset-normalizer==3.4.6
contourpy==1.3.3
curl_cffi==0.16.3
cycler==0.12.1
fonttools==4.64.0
idna==3.11
iniconfig==2.3.0
joblib==1.6.0
kiwisolver==1.5.1
lxml==6.1.3
matplotlib==3.11.1
multitasking==0.0.13
narwhals==2.26.0
numpy==2.4.6
packaging==24.0
pandas==3.0.5
peewee==4.5.1
pillow==12.3.0
platformdirs==4.11.7
pluggy==1.6.0
protobuf==7.36.1
pyarrow==25.0.1
pyparsing==3.1.1
pytest==9.1.1
python-dateutil==2.9.0.post0
pytz==2026.3.post1
requests==2.33.1
scikit-learn==1.9.0
scipy==1.17.1
six==1.16.0
soupsieve==2.9.2
threadpoolctl==3.6.0
typing_extensions==4.16.0
urllib3==2.6.3
websockets==17.1
yfinance==1.7.0
